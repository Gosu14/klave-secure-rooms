# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build uploader`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t uploader`.
FROM docker.io/node:20-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system uploader && \
    adduser --system -G uploader uploader

COPY dist/apps/uploader uploader
RUN chown -R uploader:uploader .

CMD [ "node", "uploader" ]
